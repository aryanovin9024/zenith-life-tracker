# Copy to .env and fill with your Supabase project's values
# (Project Settings → API). These are exposed to the browser by design;
# privacy comes from RLS + the login gate, not from hiding the anon key.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
